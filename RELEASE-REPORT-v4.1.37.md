# VAcleaner v4.1.37 — BOOKING ADDRESS DESKTOP OVERFLOW FIX

- Виправлено падіння `final desktop visual audit` на 1440 і 1280 px через довгу адресу доставки в картці бронювання.
- Клікабельна адреса більше не розтягує внутрішній `span` за межі viewport: текст займає ширину своєї колонки й акуратно скорочується через ellipsis.
- Повна адреса в деталях бронювання та перехід у навігатор залишаються без змін.
- Додано окремий regression-test, щоб цей desktop overflow не повернувся в наступних версіях.
