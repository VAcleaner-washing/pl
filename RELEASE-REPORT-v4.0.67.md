# VAcleaner v4.0.67 — Expense Date & UAH Field Fix

- Expense date now renders in a locale-stable `DD.MM.YYYY` display while preserving the native date picker.
- Removed duplicated/vertical `₴` prefix from the amount field; a single `грн` suffix remains.
- No finance calculation, category, backend, or settlement logic changed.
