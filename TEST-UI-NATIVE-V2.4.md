# VAcleaner · Native UI V2.4 Visual Polish RC

Route: `/admin/bronuvannia-native-v24/`

V2.4 is a screenshot-driven visual cleanup over V2.3 functional parity. It keeps all production handlers and business rules intact.

## Visual fixes
- booking filter rail no longer overlays card headers while scrolling;
- completed/cancelled cards use tighter history density and omit redundant neutral zero-balance row;
- audit disclosure is flattened and reload is a proper mobile touch target;
- booking More action sheet is a single native-style list;
- connection state is merged into profile metadata;
- issue/finance context strip uses one thin shell;
- campaign, analytics, calendar, settings and SMS microcopy readability improved;
- PWA update prompt is compact above bottom navigation.

## Preserved
- V2.3 status/action parity;
- Finance category filter;
- booking audit history;
- detail `Ще` actions;
- deposit/comment truth in compact historical cards;
- scoped offline/push/deep-link behavior;
- production v4.2.47 files and all business logic.
