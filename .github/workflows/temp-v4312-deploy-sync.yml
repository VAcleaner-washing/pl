name: TEMP v4312 deploy sync
on:
  push:
    branches:
      - qa/v4.3.12-admin-time-logistics
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: qa/v4.3.12-admin-time-logistics
          fetch-depth: 0
      - run: python scripts/temp_sync_v4312_deploy.py
      - run: |
          rm scripts/temp_sync_v4312_deploy.py
          rm .github/workflows/temp-v4312-deploy-sync.yml
          git config user.name "VAcleaner-washing"
          git config user.email "vacleaner.washing@gmail.com"
          git add -A
          git commit -m "fix: sync admin bookings v4 deploy fallback"
          git push origin HEAD:qa/v4.3.12-admin-time-logistics
