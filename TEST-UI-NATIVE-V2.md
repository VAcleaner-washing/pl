# VAcleaner · Native UI V2 Test

Routes:
- Production: `/admin/bronuvannia/`
- Native V1: `/admin/bronuvannia-native-test/`
- Native V2: `/admin/bronuvannia-native-v2/`

V2 is a cleanup pass over the approved Native Dark direction. It removes double/nested controls, repairs booking `Ще`, tightens active booking cards, simplifies settings tabs/forms, compacts calendar/client surfaces, normalizes display-only all-caps names, and removes duplicate detail information. Business logic is shared with production and is not forked.
