import fs from 'node:fs';
import path from 'node:path';
const root=process.cwd();
const read=rel=>fs.readFileSync(path.join(root,rel),'utf8');
const checks=[];
const ok=(name,condition)=>{checks.push([name,Boolean(condition)]);if(!condition)console.error(`FAIL ${name}`)};
const release=JSON.parse(read('release.json'));
const runtime=read('assets/address-autocomplete.js');
const admin=read('assets/admin-v250.js');
const edge=read('supabase/functions/vacleaner-address-v1/index.ts');
const booking=read('_next/static/chunks/146ntlcv_t6~w-v4041.js');
ok('release 4.1.35',release.version==='4.1.35'&&Number(release.build)===4135);
ok('search covers Poltava plus suburbs',edge.includes('SERVICE_RADIUS_KM = 30')&&edge.includes('SERVICE_BBOX')&&edge.includes('inServiceArea'));
ok('settlement is kept in suggestion',edge.includes('settlementOf')&&edge.includes('areaType')&&edge.includes('distanceKm'));
ok('UI explains suburb lookup',runtime.includes('Полтаву та передмістя'));
ok('access details are separate',runtime.includes('Під’їзд / квартира / поверх')&&runtime.includes('домофон 24'));
ok('general booking comment stays',booking.includes('Коментар ')&&booking.includes('Що плануєте чистити або яку хімію підібрати'));
ok('navigator keeps normalized suburb address',admin.includes("parts.length>=3")&&admin.includes("?address:`Полтава, ${address}`"));
ok('navigator still strips access details',admin.includes("full.split(' · ')[0].trim()"));
if(checks.some(([,pass])=>!pass))process.exit(1);
console.log(`v4.1.35 suburb address regression: ${checks.length}/${checks.length} OK`);
